export const metadata = {
  title: "Contact | GUMUH",
  description:
    "Contact GUMUH for inquiries, support, or partnership opportunities.",
  // Add other metadata as needed
};

export default function Layout({ children }) {
  return <>{children}</>;
}
